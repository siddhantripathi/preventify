# Preventify

Monorepo structure:
- apps/mobile: React Native app (iOS/Android)
- apps/desktop: React app (desktop)
- apps/backend: API server
- packages/shared: shared types and utilities

## Quick start
1) Copy .env.example to .env and fill values
2) Install dependencies (once projects are scaffolded)
3) Run apps from their respective folders
